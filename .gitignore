node_modules/
dist/
*.log
.DS_Store
Thumbs.db
app/web/dist/
app/server/dist/
app/shared/dist/
# transient importer/atomic-write artifacts
board/**/*.tmp-*
.vite/

# ── Private, machine-local data (NEVER committed) ──────────────────────────────
# This tracker is localhost-only; task content and project names are private and
# must not be exposed if the repo is ever published. board.config.json is created
# locally from the tracked board.config.json.template on first server startup.
board.config.json
board/
exports/
graphs/
project-docs/
projects.json
projects.json.tmp-*

# Per-machine agent settings (local permission allowlists — not shared)
**/settings.local.json
.mcp.json
import-source/
